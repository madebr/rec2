#include <windows.h>

int* gAFE = (int*)0x0074d3dc;


int WinMain (HINSTANCE.conflict hInstance, HINSTANCE.conflict hPrevInstance, LPSTR.conflict lpCmdLine, int nShowCmd)
{
    GameMain(1, "Carmageddon");
}
